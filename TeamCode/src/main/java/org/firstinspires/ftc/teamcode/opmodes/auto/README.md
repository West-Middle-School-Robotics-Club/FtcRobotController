# Autonomous OpModes

Autonomous OpModes go in this folder. Follow the same pattern as
`opmodes/teleop/ExampleTeleOp.java`: create a `Robot`, wait for start, then call subsystem methods.

Our first one is `ParkAuto` (issue #6).
